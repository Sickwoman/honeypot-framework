# Honeypot Framework - Python Dependencies
# Install with: pip install -r requirements.txt
#
# Everything below the CORE section is imported by code in this repo. Packages
# that were declared but never imported are listed under OPTIONAL, commented
# out, so a fresh install stays small and fast.
#
# Dev/test toolchain lives in requirements-dev.txt.

################################################################################
# CORE - required to run the API and ingestion pipeline
################################################################################

# Web framework (api/alerts_service.py, api/auth_routes.py, api/middleware.py)
Flask==2.3.3
Werkzeug==2.3.7

# Production WSGI server (see docker/api.Dockerfile)
gunicorn==21.2.0

# Authentication: JWT signing (api/auth.py), password hashing (api/user_manager.py)
PyJWT==2.10.1
bcrypt==4.0.1

# Configuration (api/config.py)
python-dotenv==1.0.0

# YAML: playbook definitions, sentinel policy, correlation rules
pyyaml==6.0.1

# HTTP client: threat-intel lookups, notifiers, report generators
requests==2.31.0
urllib3==2.0.7

################################################################################
# LOG AGGREGATION & MONITORING
################################################################################

# Elasticsearch client (scripts/es_client.py)
elasticsearch==8.10.0

# Prometheus metrics (scripts/prometheus-exporter.py, scripts/deception-sentinel.py)
prometheus-client==0.18.0

################################################################################
# ANALYTICS & ML (scripts/ml-*.py, scripts/advanced-analytics.py)
################################################################################

pandas==2.1.3
numpy==1.26.4
scikit-learn==1.3.2

################################################################################
# REPORTING (scripts/generate-pdf-report.py)
################################################################################

reportlab==4.0.7

################################################################################
# OPTIONAL - not imported by any code in this repo today.
# Uncomment the block you need; each notes what would use it.
################################################################################

# PostgreSQL instead of the built-in sqlite3 (api/user_manager.py,
# api/alerts_service.py use the stdlib sqlite3 directly today).
# psycopg2-binary==2.9.9   # >=2.9.9 required for Python 3.12 wheels
# SQLAlchemy==2.0.23
# Alembic==1.12.1

# Cross-origin requests, if you serve the frontend from a different origin
# than the API (docker-compose.yml proxies instead, so this isn't needed).
# Flask-CORS==4.0.0

# Attack-graph visualization (analytics/attack_graph.py works without it;
# only the optional to_networkx() helper uses it).
# networkx==3.2.1

# Excel export, if you extend the report generators beyond CSV/PDF.
# openpyxl==3.1.5
# xlsxwriter==3.1.8

# Statistical modeling, if you extend scripts/ml-trend-prediction.py beyond
# scikit-learn's LinearRegression.
# statsmodels==0.14.0
# scipy==1.11.4

# AWS SDK, if you add programmatic S3/CloudWatch calls (Terraform handles
# provisioning today).
# boto3==1.29.7

# Richer Slack/Discord clients (scripts/*-notifier.py post to webhooks with
# plain `requests` today).
# slack-sdk==3.26.1
# discord-webhook==1.3.0

################################################################################
# VERSION NOTES
################################################################################

# Last Updated: 2026-09-10
# Python: 3.11 - 3.12 (tested in CI)
#
# Installation:
#   pip install -r requirements.txt
#
# Development (adds pytest, ruff, mypy):
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Check for known vulnerabilities:
#   pip install pip-audit && pip-audit
