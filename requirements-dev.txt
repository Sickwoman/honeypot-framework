# Honeypot Framework - Development & Test Dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt
#
# Kept separate from requirements.txt so running the framework doesn't pull in
# the test and lint toolchain.

################################################################################
# TESTING
################################################################################

pytest==7.4.3
pytest-cov==4.1.0
pytest-flask==1.3.0

# Mocking / HTTP stubbing
responses==0.24.1
mock==5.1.0

################################################################################
# LINTING & TYPE CHECKING
################################################################################

# ruff replaces flake8/isort/pylint for linting (config in pyproject.toml)
ruff==0.6.9

mypy==1.7.1
types-requests==2.31.0.10
types-PyYAML==6.0.12.12
